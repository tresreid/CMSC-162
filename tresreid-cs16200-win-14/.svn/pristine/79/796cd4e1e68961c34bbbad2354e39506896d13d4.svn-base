#!/bin/bash

if [ $# -gt 1 ]
then
    echo "ERROR: Wrong number of parameters."
    echo "Usage: $0 [name]"
    exit 1
fi

if [ $# -eq 1]
then
    echo "Hello, $1"
elif [ $# -eq 0]
then
    echo "Hello, world!"
fi

exit 0
