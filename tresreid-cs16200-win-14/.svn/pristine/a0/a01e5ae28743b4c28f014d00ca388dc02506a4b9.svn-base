#include <stdio.h>
#include <string.h>
#include "power.h"

unsigned power(unsigned y, unsigned m); 

int main( int argc, char *argv[]){
    long x;
    long n;
    sscanf( argv[1],"%ld", &x);
    sscanf( argv[2],"%ld", &n);
    unsigned z = power(x,n);
    printf("%u\n",z);
    
}
